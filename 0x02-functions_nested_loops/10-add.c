#include "main.h"
#include <stdio.h>
/**
 * add - adds two integers and returns the result
 *
 * @x: number added
 *
 * @y: second number added
 * Return: Always 0
 */

int add(int x, int y)
{
	return (x + Y);
}
